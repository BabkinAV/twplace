import React from 'react';
import { StyledSubtotal } from './Subtotal.styles';

const Subtotal = ({className}: {className: string}) => {
  return <StyledSubtotal className={className}>subtotal component</StyledSubtotal>;
};

export default Subtotal;
