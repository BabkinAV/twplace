import React from 'react'
import { StyledIconButton } from './IconButton.styles'

const IconButton = () => {
	return (
		<StyledIconButton>IconButton</StyledIconButton>
	)
}

export default IconButton